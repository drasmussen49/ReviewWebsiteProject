@model ReviewWebsiteProject.Models.Review
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
    <link rel="stylesheet" href="~/css/StyleSheet.css" />
    <link href="https://fonts.googleapis.com/css?family=Tomorrow&display=swap" rel="stylesheet">
</head>
<body>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>@Model.Product.Name Review</h4>
        <div class="form-group">
            <label class="control-label">Reviewer Name:</label>
            <label class="control-label">@Model.Name</label>
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        <dl class="row">
            <dt class="col-sm-2">
                <p>The Review:</p>
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Content)
            </dd>
        </dl>

        <form asp-action="Delete" method="post">
            <input type="submit" value="Delete" class="btn btn-white btn-animated" />
            <input type="hidden" value="@Model.ProductId" asp-for="ProductId">
            <a asp-action="Index"></a>
        </form>
        <br />
        <a href="/Product/Details/@Model.ProductId" class="btn btn-white btn-animated">Go Back</a>
    </div>
</body>
</html>
